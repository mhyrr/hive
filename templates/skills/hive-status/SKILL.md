---
name: hive-status
description: Show full HIVE system status — identity, projects, tickets, scheduled jobs, agents, recent memory.
user_invocable: true
---

Call the `hive_status` MCP tool, then output the full results directly as your response text. Do not summarize or abbreviate — print the entire output so the user sees everything without expanding.
